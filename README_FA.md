# 🎬 ربات تلگرام دانلودر یوتیوب با تولید خودکار زیرنویس هوش مصنوعی

<div align="center">

[![English](https://img.shields.io/badge/English-🇺🇸-blue?style=for-the-badge)](README.md)
[![فارسی](https://img.shields.io/badge/فارسی-🇮🇷-green?style=for-the-badge)](README_FA.md)

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Telegram](https://img.shields.io/badge/Telegram-Bot-blue.svg)](https://telegram.org/)
[![YouTube](https://img.shields.io/badge/YouTube-Downloader-red.svg)](https://youtube.com/)

**🚀 ربات حرفه‌ای تلگرام برای دانلود ویدیو و صوت یوتیوب با تولید خودکار زیرنویس به کمک هوش مصنوعی**

</div>

---

## 📖 معرفی

یک **ربات تلگرام قدرتمند و پیشرفته** برای دانلود **ویدیوها و فایل‌های صوتی یوتیوب** با قابلیت **تولید خودکار زیرنویس با هوش مصنوعی**. این ربات با Python و aiogram ساخته شده و راه‌حل کاملی برای مدیریت محتوای یوتیوب با ویژگی‌های پیشرفته مانند سیستم اعتبار، برنامه زیرمجموعه‌گیری، مدیریت اشتراک و ساخت خودکار زیرنویس با استفاده از **faster-whisper** و **WhisperX** ارائه می‌دهد.

**👨‍💻 ساخته شده توسط [حسین قربانی](https://github.com/hosseinghorbani0)**

### 🎯 نکات کلیدی

- ⚡ **سریع و قابل اعتماد** - دانلود ویدیوهای یوتیوب با گزینه‌های کیفیت مختلف
- 🎵 **استخراج صوت** - تبدیل ویدیوها به فرمت MP3
- 🤖 **تولیدکننده زیرنویس هوش مصنوعی** - تولید خودکار زیرنویس به فارسی و انگلیسی
- 💳 **سیستم اعتبار** - مدل استفاده مبتنی بر اعتبار انعطاف‌پذیر
- 🔗 **برنامه زیرمجموعه‌گیری** - سیستم زیرمجموعه‌گیری داخلی برای رشد کاربران
- 💎 **سیستم اشتراک** - پلن‌های دسترسی نامحدود ماهانه
- 🛡️ **پنل مدیریت** - داشبورد کامل مدیریت برای مدیریت ربات
- 📢 **مدیریت اسپانسر** - عملکرد عضویت اجباری در کانال

---

## ✨ ویژگی‌ها

### 🎥 برای کاربران نهایی

#### دانلود یوتیوب
- ✅ **گزینه‌های کیفیت متعدد** - دانلود با کیفیت 480p، 720p، 1080p
- ✅ **استخراج صوت** - استخراج صوت به صورت فایل‌های MP3
- ✅ **پشتیبانی از YouTube Shorts** - دانلود ویدیوهای YouTube Shorts
- ✅ **محدودیت مدت زمان** - حداکثر 30 دقیقه برای هر ویدیو
- ✅ **انتخاب هوشمند فرمت** - انتخاب خودکار بهترین کیفیت
- ✅ **پشتیبانی از فایل‌های بزرگ** - یکپارچه‌سازی Pyrogram برای فایل‌های بیشتر از 49MB

#### تولید زیرنویس هوش مصنوعی
- ✅ **تبدیل خودکار گفتار به متن** - تبدیل گفتار به متن با هوش مصنوعی
- ✅ **پشتیبانی چندزبانه** - زیرنویس فارسی و انگلیسی
- ✅ **هم‌ترازی در سطح کلمه** - زمان‌بندی دقیق با WhisperX
- ✅ **خروجی فرمت SRT** - فرمت استاندارد فایل زیرنویس
- ✅ **شتاب GPU** - پردازش سریع با پشتیبانی CUDA (اختیاری)

#### سیستم اعتبار و اشتراک
- ✅ **اعتبار رایگان** - اعتبار اولیه برای کاربران جدید
- ✅ **پاداش زیرمجموعه‌گیری** - کسب اعتبار با دعوت دوستان
- ✅ **اشتراک نامحدود** - پلن‌های اشتراک ماهانه
- ✅ **کدهای ریدیم** - کدهای یک‌بارمصرف برای اشتراک
- ✅ **ردیابی اعتبار** - نظارت بر وضعیت اعتبار در زمان واقعی

### 👨‍💼 برای مدیران

#### داشبورد مدیریت
- ✅ **ورود امن** - احراز هویت با نام کاربری و رمز عبور
- ✅ **آمار کاربران** - تعداد کل کاربران ثبت‌شده
- ✅ **تولیدکننده کد ریدیم** - ساخت کدهای اشتراک
- ✅ **مدیریت کد** - ردیابی استفاده و انقضای کد

#### مدیریت اسپانسر
- ✅ **مدیریت کانال** - افزودن/حذف کانال‌های اسپانسر (حداکثر 6)
- ✅ **سیستم عضویت اجباری** - الزام به عضویت در کانال
- ✅ **تأیید خودکار** - بررسی وضعیت عضویت کاربر
- ✅ **گزینه‌های دور زدن** - ادمین‌ها و مشترکین از عضویت اجباری معاف هستند

---

## 🏗️ ساختار پروژه

```
├── main.py                  # نقطه ورود اصلی ربات
├── config.py                # تنظیمات پیکربندی
├── database.py              # مدیریت دیتابیس SQLite
├── download.py              # هندلر دانلود یوتیوب
├── pyrogram_client.py       # هندلر آپلود فایل‌های بزرگ
├── keyboards.py             # کیبوردهای اینلاین و ریپلای
├── states.py                # حالت‌های FSM برای مکالمات
├── admin.py                 # عملکرد پنل مدیریت
├── api_security.py          # توابع امنیتی API
├── audio_to_subtitle.py     # تولید زیرنویس هوش مصنوعی
├── credits.py               # سیستم اعتبار و زیرمجموعه‌گیری
├── force_join.py            # هندلر عضویت اجباری در کانال
├── sponsor.py               # مدیریت اسپانسر
├── user_agents.py           # لیست چرخش User-Agent
├── requirements.txt         # وابستگی‌های اصلی
└── requirements-optional.txt # وابستگی‌های اختیاری
```

---

## 📋 نیازمندی‌ها

### نیازمندی‌های سیستم

- **Python** 3.8 یا بالاتر
- **FFmpeg** (برای پردازش صدا/ویدیو)
- **SQLite3** (شامل پایتون است)

### وابستگی‌های پایتون

**وابستگی‌های اصلی:**
```bash
aiogram==3.13.1          # فریمورک مدرن ربات تلگرام
yt-dlp==2024.11.16       # دانلودر یوتیوب
python-dotenv==1.0.1     # متغیرهای محیطی
```

**وابستگی‌های اختیاری (برای تولید زیرنویس):**
```bash
faster-whisper          # تشخیص گفتار سریع
whisperx                # هم‌ترازی در سطح کلمه
torch                   # PyTorch برای شتاب GPU
googletrans==4.0.0-rc1  # سرویس ترجمه
```

---

## 🚀 راهنمای شروع سریع

### مرحله 1: کلون کردن مخزن

```bash
git clone https://github.com/hosseinghorbani0/youtube-telegram-bot-ai-subtitle.git
cd youtube-telegram-bot-ai-subtitle
```

### مرحله 2: نصب وابستگی‌ها

```bash
# نصب وابستگی‌های اصلی
pip install -r requirements.txt

# نصب وابستگی‌های اختیاری (برای تولید زیرنویس)
pip install -r requirements-optional.txt
```

### مرحله 3: نصب FFmpeg

**Windows:**
- دانلود از [ffmpeg.org](https://ffmpeg.org/download.html)
- افزودن به PATH سیستم

**Linux:**
```bash
sudo apt-get install ffmpeg
# یا
sudo yum install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

### مرحله 4: پیکربندی محیط

ساخت فایل `.env` در ریشه پروژه:

```env
BOT_TOKEN=your_telegram_bot_token_here
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash
```

**نحوه دریافت اعتبارنامه:**

1. **BOT_TOKEN:** ساخت ربات از طریق [@BotFather](https://t.me/BotFather) در تلگرام
2. **API_ID & API_HASH:** دریافت از [my.telegram.org](https://my.telegram.org/apps)

### مرحله 5: تنظیم تنظیمات

ویرایش `config.py`:

```python
# تنظیمات ربات
BOT_USERNAME = "your_bot_username"  # بدون @

# تنظیمات ادمین
ADMIN_USERNAME = "your_admin_username"
ADMIN_PASSWORD = "your_admin_password"
ADMIN_CARD_NUMBER = "your_card_number"
ADMIN_PAYMENT_ID = "@your_telegram_id"

# تنظیمات اشتراک
SUBSCRIPTION_PRICE = "50,000 تومان"

# تنظیمات اعتبار
INITIAL_CREDITS = 5
REFERRAL_BONUS_CREDITS = 1

# تنظیمات دانلود
MAX_FILE_SIZE = 49 * 1024 * 1024  # 49 مگابایت
MAX_DURATION = 1800  # 30 دقیقه
```

### مرحله 6: اجرای ربات

```bash
python main.py
```

دیتابیس (`bot_data.db`) به صورت خودکار در اولین اجرا ساخته می‌شود.

---

## 📚 مستندات فنی

### کامپوننت‌های اصلی

#### 1. `main.py` - نقطه ورود ربات

راه‌اندازی اصلی ربات و ثبت هندلرها.

**هندلرهای کلیدی:**
- `cmd_start()` - مدیریت دستور `/start` با پشتیبانی از لینک زیرمجموعه‌گیری
- `cmd_admin()` - دسترسی به پنل مدیریت
- `msg_youtube_link()` - پردازش URL یوتیوب
- `cb_quality()` - هندلر کالبک انتخاب کیفیت

**مدیریت حالت:**
- استفاده از `aiogram.fsm` برای مدیریت حالت مکالمه
- حالت‌ها در `states.py` تعریف شده‌اند

#### 2. `database.py` - مدیریت دیتابیس

عملیات دیتابیس SQLite برای کاربران، اعتبارها، اشتراک‌ها، کدهای ریدیم و اسپانسرها.

**ساختار دیتابیس:**

```sql
-- جدول کاربران
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    referrer_id INTEGER,
    credits INTEGER DEFAULT 5,
    subscription_end REAL DEFAULT 0
)

-- جدول کدهای ریدیم
CREATE TABLE redeem_codes (
    code TEXT PRIMARY KEY NOT NULL,
    is_used INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP,
    used_by_id INTEGER
)

-- جدول اسپانسرها
CREATE TABLE sponsors (
    channel_handle TEXT PRIMARY KEY NOT NULL,
    channel_link TEXT NOT NULL
)
```

**توابع کلیدی:**
- `initialize_database()` - ساخت تمام جداول
- `add_user()` - ثبت کاربر جدید
- `add_credits()` / `deduct_credits()` - مدیریت اعتبار
- `is_subscribed()` - بررسی وضعیت اشتراک
- `create_and_store_redeem_code()` - تولید کدهای ریدیم
- `get_sponsors()` / `add_sponsor()` / `remove_sponsor()` - مدیریت اسپانسرها

#### 3. `download.py` - هندلر دانلود یوتیوب

دانلود ویدیو/صوت یوتیوب با استفاده از `yt-dlp`.

**توابع کلیدی:**
- `process_youtube_link()` - استخراج اطلاعات ویدیو و نمایش گزینه‌های کیفیت
- `handle_quality_callback()` - پردازش انتخاب کیفیت و دانلود
- `download_video_sync()` - دانلود همگام با منطق تلاش مجدد
- `get_download_opts()` - پیکربندی گزینه‌های yt-dlp

**ویژگی‌ها:**
- چرخش تصادفی User-Agent برای دور زدن محدودیت
- چندین فرمت پشتیبان برای قابلیت اطمینان
- یکپارچه‌سازی Pyrogram برای فایل‌های بزرگ (>49MB)
- پاکسازی خودکار فایل پس از آپلود

#### 4. `audio_to_subtitle.py` - تولیدکننده زیرنویس هوش مصنوعی

تولید زیرنویس پیشرفته با استفاده از faster-whisper و هم‌ترازی اختیاری WhisperX.

**توابع کلیدی:**
- `transcribe_pipeline()` - تابع اصلی تولید زیرنویس
- `transcribe_with_faster_whisper()` - تبدیل گفتار به متن
- `align_with_whisperx()` - هم‌ترازی در سطح کلمه (اختیاری)
- `build_subtitles()` - فرمت‌بندی زیرنویس‌ها به فرمت SRT

**پیکربندی:**
- انتخاب مدل بر اساس زبان (فارسی/انگلیسی)
- پشتیبانی از شتاب GPU (CUDA)
- قابل پیکربندی از طریق متغیرهای محیطی

#### 5. `credits.py` - سیستم اعتبار و زیرمجموعه‌گیری

مدیریت اعتبار کاربران، زیرمجموعه‌ها و اشتراک‌ها.

**جریان اعتبار:**
1. کاربران جدید `INITIAL_CREDITS` دریافت می‌کنند (پیش‌فرض: 5)
2. هر دانلود 1 اعتبار مصرف می‌کند (اگر اشتراک فعال نباشد)
3. معرف‌ها `REFERRAL_BONUS_CREDITS` (پیش‌فرض: 1) برای هر زیرمجموعه دریافت می‌کنند
4. کاربران دارای اشتراک دانلود نامحدود دارند

#### 6. `admin.py` - پنل مدیریت

عملکرد کامل مدیریت برای مدیریت ربات.

**ویژگی‌ها:**
- سیستم ورود امن
- آمار کاربران
- تولید کد ریدیم
- احراز هویت مبتنی بر نشست

#### 7. `force_join.py` - سیستم عضویت اجباری

اجبار به عضویت در کانال قبل از اجازه استفاده از ربات.

**شرایط دور زدن:**
- ادمین‌ها (کاربران احراز هویت شده)
- کاربران دارای اشتراک فعال

---

## 🎮 استفاده

### دستورات کاربر

- `/start` - شروع ربات
  - پشتیبانی از لینک‌های زیرمجموعه‌گیری: `/start 123456` یا `/start ref=123456`

### دکمه‌های منو

- **📥 دانلود یوتیوب** - دانلود ویدیو یوتیوب
- **⭐ وضعیت اعتبار** - مشاهده وضعیت اعتبار
- **🔗 دریافت لینک زیرمجموعه** - دریافت لینک زیرمجموعه‌گیری
- **💳 خرید اشتراک** - خرید اشتراک

### دستورات ادمین

- `/admin` - دسترسی به پنل مدیریت

**ویژگی‌های پنل مدیریت:**
- **🎁 ساخت ریدیم کد** - تولید کدهای ریدیم
- **📢 مدیریت اسپانسرها** - مدیریت کانال‌های اسپانسر
- **🔒 خروج از پنل** - خروج از پنل مدیریت

---

## 🔧 پیکربندی

### متغیرهای محیطی

| متغیر | توضیحات | الزامی |
|-------|---------|--------|
| `BOT_TOKEN` | توکن ربات تلگرام | ✅ بله |
| `API_ID` | شناسه API تلگرام | ✅ بله |
| `API_HASH` | هش API تلگرام | ✅ بله |

### تنظیمات Config.py

| تنظیم | پیش‌فرض | توضیحات |
|-------|---------|---------|
| `INITIAL_CREDITS` | 5 | اعتبار اولیه برای کاربران جدید |
| `REFERRAL_BONUS_CREDITS` | 1 | اعتبار برای هر زیرمجموعه |
| `SUBSCRIPTION_DURATION_DAYS` | 30 | مدت اشتراک |
| `MAX_FILE_SIZE` | 49 MB | حداکثر اندازه فایل |
| `MAX_DURATION` | 1800 ثانیه | حداکثر مدت زمان ویدیو |

---

## 🐛 عیب‌یابی

### مشکلات رایج

**1. ربات شروع نمی‌شود**
- تأیید کنید که `BOT_TOKEN` در فایل `.env` تنظیم شده باشد
- بررسی اعتبار توکن

**2. دانلود ناموفق است**
- بررسی اتصال اینترنت
- تأیید فرمت لینک یوتیوب
- اطمینان از دسترسی ویدیو (خصوصی/محدود نباشد)

**3. خطاهای دیتابیس**
- بررسی دسترسی نوشتن در دایرکتوری پروژه
- اطمینان از اینکه `bot_data.db` قفل نشده باشد

**4. تولید زیرنویس کار نمی‌کند**
- نصب وابستگی‌های اختیاری: `pip install -r requirements-optional.txt`
- اطمینان از نصب FFmpeg و وجود در PATH
- بررسی دسترسی GPU (اختیاری)

---

## 📝 مجوز

این پروژه تحت مجوز **MIT** منتشر شده است.

---

## 🤝 مشارکت

مشارکت‌ها خوش‌آمد هستند! لطفاً درخواست Pull Request ارسال کنید.

**نحوه مشارکت:**
1. Fork کردن مخزن
2. ساخت شاخه ویژگی (`git checkout -b feature/AmazingFeature`)
3. Commit تغییرات (`git commit -m 'Add some AmazingFeature'`)
4. Push به شاخه (`git push origin feature/AmazingFeature`)
5. باز کردن Pull Request

---

## 📧 پشتیبانی

برای سوالات، مشکلات یا پشتیبانی:
- باز کردن issue در [GitHub Issues](https://github.com/hosseinghorbani0/youtube-telegram-bot-ai-subtitle/issues)
- بررسی issues و بحث‌های موجود

---

## 🙏 تشکر

با تشکر ویژه از جامعه منبع باز:

- [aiogram](https://github.com/aiogram/aiogram) - فریمورک مدرن ربات تلگرام
- [yt-dlp](https://github.com/yt-dlp/yt-dlp) - دانلودر یوتیوب
- [faster-whisper](https://github.com/guillaumekln/faster-whisper) - تشخیص گفتار سریع
- [WhisperX](https://github.com/m-bain/whisperX) - هم‌ترازی در سطح کلمه

---

## ⚠️ سلب مسئولیت

این ربات فقط برای **اهداف آموزشی** است. لطفاً شرایط استفاده یوتیوب و قوانین کپی‌رایت را رعایت کنید. کاربران مسئول اطمینان از داشتن حق دانلود محتوا هستند.

---

## 🌟 تاریخچه ستاره

اگر این پروژه برای شما مفید است، لطفاً در GitHub به آن ⭐ بدهید!

---

<div align="center">

[![English](https://img.shields.io/badge/English-🇺🇸-blue?style=for-the-badge)](README.md)
[![فارسی](https://img.shields.io/badge/فارسی-🇮🇷-green?style=for-the-badge)](README_FA.md)

**ساخته شده با ❤️ برای جامعه تلگرام**

[![GitHub stars](https://img.shields.io/github/stars/hosseinghorbani0/youtube-telegram-bot-ai-subtitle?style=social)](https://github.com/hosseinghorbani0/youtube-telegram-bot-ai-subtitle)
[![GitHub forks](https://img.shields.io/github/forks/hosseinghorbani0/youtube-telegram-bot-ai-subtitle?style=social)](https://github.com/hosseinghorbani0/youtube-telegram-bot-ai-subtitle)

---

## 👨‍💻 سازنده

**حسین قربانی**

- 🌐 وب‌سایت: [hosseinghorbani0.ir](https://hosseinghorbani0.ir)
- GitHub: [@hosseinghorbani0](https://github.com/hosseinghorbani0)
- Telegram: [@nicot_10](https://t.me/nicot_10)

</div>
